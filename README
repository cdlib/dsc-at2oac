XSLT 1.0 to process archivists' toolkit 2.x output to get it
ready for submission to oac.cdlib.org with voroEAD

To run this XSLT you can use libxml2/libxslt http://xmlsoft.org/XSLT/

	xsltproc --output VORO_INPUT.xml at2oac.xsl AT_OUTPUT.xml

OR you can use saxon http://saxon.sourceforge.net/

	java -jar saxonb-x.x.jar -o VORO_INPUT.xml AT_OUTPUT at2oac.xsl

